// Créer un tableau et ajouter des éléments.
// Afficher les éléments du tableau avec les boucles.


var ageetudiant = [
    10, 29, 20, 18, 40
];

for(var i=0; i<ageetudiant.length; i++) {
    console.log(ageetudiant[i]);
};